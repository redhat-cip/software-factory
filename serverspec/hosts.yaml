redmine:
  :roles:
    - base
    - redmine
  :username: root
  :hostname: redmine.tests.dom

mysql:
  :roles:
    - base
    - mysql
  :username: root
  :hostname: mysql.tests.dom

ldap:
  :roles:
    - base
    - ldap
  :username: root
  :hostname: ldap.tests.dom

gerrit:
  :roles:
    - base
    - gerrit
  :username: root
  :hostname: gerrit.tests.dom

jenkins:
  :roles:
    - base
    - jenkins
  :username: root
  :hostname: jenkins.tests.dom

commonservices:
  :roles:
    - base
    - commonservices
  :username: root
  :hostname: commonservices.tests.dom
