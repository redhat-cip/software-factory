redmine:
  :roles:
    - base
    - redmine
    - postfix
    - ntp
  :username: root
  :hostname: redmine

mysql:
  :roles:
    - base
    - mysql
    - postfix
    - ntp
  :username: root
  :hostname: mysql

gerrit:
  :roles:
    - base
    - gerrit
    - postfix
    - ntp
  :username: root
  :hostname: gerrit

jenkins:
  :roles:
    - base
    - jenkins
    - postfix
    - ntp
  :username: root
  :hostname: jenkins

managesf:
  :roles:
    - base
    - managesf
    - commonservices
    - ntp
  :username: root
  :hostname: managesf
