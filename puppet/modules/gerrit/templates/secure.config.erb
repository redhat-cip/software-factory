[database]
    password = <%= @gerrit_mysql_secret %>
[ldap]
    # Will be used if anonymous bind is disallowed
    password = <%= @ldap_password %>
[sendmail]
    smtpPass = <%= has_variable?("smtp_pass") ? smtp_pass: '' %>
