- project:
    name: config
    node: master
    jobs:
      - '{name}-update'
      - '{name}-check'

- project:
    name: zuul-demo
    node: <%= @settings['jenkins_default_slave'] %>
    jobs:
      - '{name}-unit-tests'
      - '{name}-functional-tests'
