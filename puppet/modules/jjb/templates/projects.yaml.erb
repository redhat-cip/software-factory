# This file won't be updated and must be updated manually

# Jobs
# ====



# Projects
# ========
- project:
    name: zuul-demo
    node: <%= @settings['jenkins_default_slave'] %>
    periodic_cmd: |
      echo "Running periodic tests"
      ./run_functional-tests.sh
    jobs:
      - '{name}-unit-tests'
      - '{name}-functional-tests'
      - 'periodic-{name}'


