#!/bin/bash
ssh root@managesf.<%= @fqdn %> cat /root/mysqldb.sql.gz | gunzip | mysql -u root -p<%= @mysql_root_pwd %>
systemctl restart mariadb
# compatibility issue, will be removed after patch 1858 is merged
ssh root@managesf.<%= @fqdn %> cat /root/alldb.sql.gz | gunzip | mysql -u root -p<%= @mysql_root_pwd %>
systemctl restart mariadb
