jenkins:
  gerrit_url: http://sf-gerrit/
  gerrit_hostname: sf-gerrit
  gerrit_username: jenkins
  ldap_url: ldap://sf-ldap
  ldap_root_dn: dc=enovance,dc=com
  jenkins_url: http://sf-jenkins:8080/
  jenkins_username: jenkins
  jenkins_password: userpass
  credential_id: JENKINS_CREDS_ID
  slaves: [sf-jenkins-slave01, sf-jenkins-slave02]
