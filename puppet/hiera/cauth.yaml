cauth:
  signin_url: "http://%{hiera('auth_url')}/login"
  signout_url: "http://%{hiera('auth_url')}/logout"
  github_redirect_url: "http://%{hiera('auth_url')}/login/github/callback"
  github_app_id: GITHUB_APP_ID
  github_app_secret: GITHUB_APP_SECRET
  ldap_account_base: LDAP_ACCOUNT_BASE
