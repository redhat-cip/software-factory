#cloud-config
output: {all: '| tee -a /var/log/cloud-init-output.log'}
runcmd:
 - ssh-keygen -t rsa -N '' -f /home/gerrit/ssh_host_rsa_key
 - echo PLACEHOLDER puppetmaster.pub >> /etc/hosts
 - puppet agent --test
