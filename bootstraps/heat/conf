# The name of the key you previously updated in your tenant
key_name=yourkeyname

# flavors (for CPU and RAM)
puppetmaster_flavor="m1.small"
mysql_flavor="m1.small"
managesf_flavor="m1.small"
gerrit_flavor="m1.small"
redmine_flavor="m1.small"
jenkins_flavor="m1.small"
slave_flavor="m1.small"

# flavors (for disk size)
puppetmaster_root_size="40"
mysql_root_size="40"
managesf_root_size="40"
gerrit_root_size="40"
redmine_root_size="40"
jenkins_root_size="40"
slave_root_size="40"

# External network UUID
ext_net_uuid="6c83db7b-480e-4198-bc69-88df6fd17e55"

# Space separated list of networks from TCP/22 is accessible
sg_admin_cidr="0.0.0.0/0"

# Space separated list of networks from ALL SF services are accessible
sg_user_cidr="0.0.0.0/0"

# Namservers IPs used by all VMs (comma separated IPs)
nameservers="8.8.8.8,8.8.4.4"
