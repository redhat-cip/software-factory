#TODO(fbo): Flavor for each VM in a config file

# Openstack API config
export OS_IDENTITY_API_VERSION=${OS_IDENTITY_API_VERSION:-2.0}
export OS_PASSWORD=${OS_PASSWORD:-wxcvbn}
export OS_AUTH_URL=${OS_AUTH_URL:-http://198.154.188.200:5000/v2.0}
export OS_USERNAME=${OS_USERNAME:-admin}
export OS_TENANT_NAME=${OS_TENANT_NAME:-admin}

# (devstack) - create a tunnel
DEVSTACK_SSH=ubuntu@devstack

PUB_KEY_PATH="$HOME/.ssh/id_rsa.pub"
ROLES="puppetmaster ldap mysql gerrit jenkins redmine edeploy jenkins-slave01"
RECREATE_ROLES=false
PREFIX="customer-$OS_TENANT_NAME"
BUILD="/tmp/$PREFIX-build/"
# (devstack) - don't set a flavor it will be automatically created
#VM_FLAVOR="m1.small"
RELEASE="D7-H.1.0.0"
SECURITY_GROUP="${PREFIX}-sg"
AUTHORIZED_NETS="***REMOVED***"
SERVICE_PORTS="22 29418 80 8080"
BUILD_ROLE_PATH="/var/lib/debootstrap/install/$RELEASE"
GIT_VOLUME_NAME=${GIT_VOLUME_NAME:-$PREFIX-gitvolume}
GIT_VOLUME_SIZE=${GIT_VOLUME_SIZE:-1}
