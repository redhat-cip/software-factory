Managing global settings with the config repository code review
===============================================================

Many parameters are presented to users through the config repository.
This enables users to propose modifications through the code review system,
and when members of the config-repo core group approve a change, it automatically applies
the configuration.

.. toctree::
   :maxdepth: 3

   swiftlogs_user
   nodepool_user
   gerritbot_user
