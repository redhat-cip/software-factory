#!/bin/sh

exec curl -s --user 'jenkins:{{ creds_jenkins_user_password }}' --data-urlencode "script=$(</usr/local/share/sf-jenkins-slave-tools/remove-offline-node.groovy)" http://jenkins.{{ fqdn }}:8080/jenkins/scriptText
