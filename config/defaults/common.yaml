# This file is part of the core configuration of SF.
# Please do not modify it.

url:
  gateway_url: "http://%{hiera('fqdn')}/"
  api_redmine_url: "http://api-redmine.%{hiera('fqdn')}:8083/"
  jenkins_url: "http://jenkins.%{hiera('fqdn')}:8081/jenkins/"
  # jenkins-job update needs this else it fails with
  # "jenkins.NotFoundException: Requested item could not be found"
  jenkins_jjb_url: "http://jenkins.%{hiera('fqdn')}:8080/jenkins/"
  zuul_url: "http://zuul.%{hiera('fqdn')}:8084/"
  gerrit_url: "http://gerrit.%{hiera('fqdn')}:8000/r/"
  redmine_pub_url: "http://%{hiera('fqdn')}/redmine/"
  jenkins_pub_url: "http://%{hiera('fqdn')}/jenkins/"
  gerrit_pub_url: "http://%{hiera('fqdn')}/r/"
  zuul_pub_url: "http://%{hiera('fqdn')}/zuul/"
  paste_pub_url: "http://%{hiera('fqdn')}/paste/"
  etherpad_pub_url: "http://%{hiera('fqdn')}/etherpad/"
